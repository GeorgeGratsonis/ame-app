# âme — web build

The compiled web build of the âme Pilates booking app, published so the studio
can open it from a phone without a development server.

This repository contains build output only. The source lives in a private
repository; nothing here is edited by hand — it is replaced wholesale by
`npx expo export --platform web` from the app project.
